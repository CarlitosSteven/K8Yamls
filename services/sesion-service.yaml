apiVersion: v1
kind: Service
metadata:
  name: sesion-service
spec:
  selector:
    app: sesion-service
  ports:
    - port: 80 #Puerto del Service dentro del cluster
      targetPort: 8080 #Puerto donde escucha el contenedor
